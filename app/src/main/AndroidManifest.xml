<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yusriyusron.practice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".shared_preferences.FormUserPreverenceActivity" />
        <activity android:name=".read_write.ReadWriteActivity" />
        <activity android:name=".sqllite.FormAddUpdateActivity" />
        <activity android:name=".sqllite.MainFormActivity" />
        <activity android:name=".pre_load.MahasiswaActivity" />
        <activity android:name=".pre_load.MainPreLoadActivity" />
    </application>

</manifest>